import React from "react";

const ImageOverlayPreview: React.FC = () => (
  <div className="enhancement-preview">
    <h4>Image Overlay Preview</h4>
    <p>Render vector overlays that mimic original typography.</p>
  </div>
);

export default ImageOverlayPreview;
