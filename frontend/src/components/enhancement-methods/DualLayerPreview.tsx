import React from "react";

const DualLayerPreview: React.FC = () => (
  <div className="enhancement-preview">
    <h4>Dual Layer Preview</h4>
    <p>Visualize combined hidden and visible text layers.</p>
  </div>
);

export default DualLayerPreview;
